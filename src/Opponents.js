// import { useState } from 'react';

export default function Opponents({ count }){

    return(
        <>
        {/* for (let i=0; i < count; count++) {
            <button>Cmputer</button>
        } */}
        </>
    );
}